
import pytest

from pytest_homeassistant_custom_component.common import MockConfigEntry

#@pytest.fixture
#def mock_lights(hass):
#    light1_entry = MockConfigEntry(domain="light", entry_id="foo_light")
